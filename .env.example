# Auto-provisioned by Netlify DB at deploy time. For local dev, paste a Neon/Postgres URL.
NETLIFY_DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Everything else (Resend, Gemini, dashboard password) is set in the in-app /onboarding screen.
# It's stored encrypted in your database.
